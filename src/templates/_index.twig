{% extends "_layouts/cp" %}

{% block content %}
    <p>{{ "Web Doctor is installed and running."|t('web-doctor') }}</p>
    <p class="light">
        {{ "Version {version}, schema version {schemaVersion}."|t('web-doctor', {
            version: version,
            schemaVersion: schemaVersion,
        }) }}
    </p>
{% endblock %}
